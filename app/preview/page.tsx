import { PreviewTool } from "@/components/preview/PreviewTool";

export const metadata = {
  title: "Preview — Ropreview",
  description: "Preview your Roblox game thumbnail in real UI surfaces.",
};

export default function PreviewPage() {
  return <PreviewTool />;
}
